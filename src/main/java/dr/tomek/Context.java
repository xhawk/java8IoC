package dr.tomek;

public interface Context {
	
	/*Repository repo = () -> {
		return "@";
	};

	Service concreteService = (name) -> {
		return "concrete" + repo.word() + name;
	};

	Service asfaltService = (name) -> {
		return "asfalt" + repo.word() + name;
	};*/
}
